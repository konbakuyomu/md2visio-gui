---
description: 
globs: 
alwaysApply: true
---
### 🛡 Global Rules
1. 所有回复必须使用 **中文**。
2. 所有路径示例使用 Windows 格式，例如 `C:\Users\Kevin\Projects\...`。
3. **先检索，后行动**：
   • 在修改/生成任何代码前，必须执行 `get-project-info`。  
   • 完成后必须执行 `update-project-info`，自动更新  
     `.codelf\project.md` 与 `.codelf\_changelog.md`。
4. 如遇陌生符号 / 跨文件引用，必须积极进行相关上下文的读取和思考，禁止猜测
5. 每次编辑代码完成后，必须调用`update-project-info`，并遵循其返回的指示
6. Mermaid图表规则：节点文本内禁用[]{}()等特殊符号，用空格或+连接词语
7. 这个项目的根目录是:D:\visio转换\md2visio

### 🔄 Workflow Framework
0. **初始化检测**  
   若 `get-project-info` 为空或返回 *project not initialized*：  
     a) 向用户确认是否运行 `init-codelf`。  
     b) 同意 → 触发 `code-inspector` 全仓扫描 → `init-codelf` → 再次 `get-project-info`。  
1. 若用户操作单文件且涉及依赖，先对工程目录其他的相关文件信息进行搜集。   

2. 遇到跨文件引用或不明符号，必须先检索(cursor的相关工具/mcp服务)，所有结论必须基于实际源码或文档验证，不得使用推测性措辞